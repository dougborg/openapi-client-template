# {{ project_name }}

Client for the {{ api_service }} API.

Usage
- Install from source: pip install -e .
- See examples in examples/ and tests/

Template notes
- This project uses `openapi-client-core` for shared retry/pagination/rate-limiting.
- Optional MCP support included when `include_mcp` is true.