openapi-client-core

A minimal shared library for common OpenAPI client concerns:
- retries (tenacity/backoff)
- rate limiting wrappers
- pagination helpers
- standard exceptions and types

This directory is a placeholder showing how to structure an extracted package.
Publish to PyPI (or use as git dependency) and reference from template projects.